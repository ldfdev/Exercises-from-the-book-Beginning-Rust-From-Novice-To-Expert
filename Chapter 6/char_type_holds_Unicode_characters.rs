
fn main() {
    
    let e_grave: char = 'è';

    let hao: char = '好';

    // char literals are represented in single quotes

    // Rust char type contais Unicode characters, i.e. are 4 bytes long

    // in Rust char is not considered numeric type

    println!("Rust char is Unicode: {} {}", e_grave, hao)
}