
fn main() {

    // enum introduces a new type
    // values in an enum are called variants
    enum DirectionChinese {
        YOU,
        SHANG,
        XIA,
        ZUO
    };

    let chosenDirection: DirectionChinese = DirectionChinese::YOU;

}